<!DOCTYPE html>
<html>
<head>
    <title>YelpCamp</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    <link rel="stylesheet" href="/customize.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="/" id="yelpCampLogo"><i class="fas fa-campground"></i>YelpCamp</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto mb-2 mb-lg-0 left">
                <li class="nav-item mr-2">
                    <a class="nav-link" href="/campgrounds" tabindex="-1">Campgrounds</a>
                </li>
                <li class="nav-item mr-2">
                    <a class="nav-link" href="/campgrounds/new" tabindex="-1">Add-new</a>
                </li>
                <% if(!currentUser){ %>
                    <li class="nav-item mr-2">
                        <a class="nav-link" href="/login" tabindex="-1">Login</a>
                    </li>
                    <li class="nav-item mr-2">
                        <a class="nav-link" href="/register" tabindex="-1">Register</a>
                    </li>
                <% }else{ %>
                    <li class="nav-item mr-2">
                        <a class="nav-link" href="/logout" tabindex="-1">Logout</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
<div class="container">
    <% if(error && error.length > 0){ %>
        <div class="alert alert-danger" role="alert"><%= error %></div>
    <% } %>
    <% if(success && success.length > 0){ %>
        <div class="alert alert-success" role="alert"><%= success %></div>
    <% } %>
</div>